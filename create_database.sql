-- Create ShoeMart database for local XAMPP MySQL
CREATE DATABASE IF NOT EXISTS `shoemart_db` CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
-- Optionally create a dedicated user (change password as needed)
-- CREATE USER 'shoemart_user'@'localhost' IDENTIFIED BY 'your_password_here';
-- GRANT ALL PRIVILEGES ON `shoemart_db`.* TO 'shoemart_user'@'localhost';
-- FLUSH PRIVILEGES;
